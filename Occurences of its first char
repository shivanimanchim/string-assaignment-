#write a pytho program to get a string from a given string where all the occurances of its first char had been changed to '$' 
except the first char itself

defv(str1):
 c=str1[0]
 str1=str1.replace(c,'$')
 str1=c+str1[1:]
 
 return str1
 print(v('restart'))
